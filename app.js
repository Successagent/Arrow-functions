/*
 * Arrow functions can have arguments too

* If an arrow functions has a single parameter, the parentheses enclosing it can be remove

*if the parentheses enclosing the function are removed you can remove the curly braces enclosing the function body and the return statement

* For you to write an arrow function, you will start with const and the name of the function then the block of the function
*/

const arrowFunc = () => 123;  // Arrow functions
